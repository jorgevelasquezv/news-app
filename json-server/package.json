{
    "name": "json-server",
    "version": "1.0.0",
    "description": "json server ",
    "main": "index.js",
    "scripts": {
        "start": "json-server db.json --port 8080",
        "dev": "json-server --watch db.json --port 8080",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "MIT",
    "dependencies": {
        "json-server": "^1.0.0-beta.2"
    }
}
