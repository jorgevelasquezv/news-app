// Exportación de un enum llamado Role, que define diferentes roles de usuario en la aplicación.
export enum Role {
  // Rol para un usuario regular.
  User = 'user',

  // Rol para un administrador con privilegios elevados.
  Admin = 'admin',
}
